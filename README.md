
  # 文書管理UI（Sharepoint対応）

  This is a code bundle for 文書管理UI（Sharepoint対応）. The original project is available at 
https://ysky24-cell.github.io/document-management-ui-demo/
デモサイト
  
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
